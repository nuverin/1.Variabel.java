const accord = document.querySelectorAll(".accordion__header");

accord.forEach((acc) => {
  acc.addEventListener("click", (ev) => {
    acc.classList.toggle("active");
    const accore = document.q
  });
});
